# Frontend environment variables example
# Copy to .env.local and fill values for your environment

# Base URL of the backend API (no trailing slash)
NEXT_PUBLIC_API_BASE=http://localhost:4000

# Optional: analytics or 3rd-party keys
# NEXT_PUBLIC_SENTRY_DSN=
# NEXT_PUBLIC_MAPBOX_TOKEN=

# Notes:
# - The refresh token is expected to be set as an httpOnly cookie by the backend.
# - Keep secrets out of your repository. Put them in .env.local or in your deployment platform's env settings.
